## Data Repository of Download_Backtest_Deploy

This repository is designed to manage the Algo Data:  

## Git Workflow

Use the following steps to manage Data versioning:

```bash
ls
cd ..
cd your-project-folder
git status
git add .
git commit -m "Your meaningful commit message"
git push origin main
